const tls = require('tls');
function Connected(stream) {
    if(stream) {
        stream.write('GET \ Http')
    }
    else{
        console.log('connection failed');
    }
}
var dummy = this;
dummy.socket = tls.connect(443 , 'encrypted.google.com');
dummy.Connected = true;
